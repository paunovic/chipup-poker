extends secure
block content
  table(border=1)
    tr
      th club name
      th seq
      th rake
    each val in clubs
      tr
        td
          a(href='/secure/club?id='+val._id)=val.name
        td=val.seq
        td=val.rake+'%'
