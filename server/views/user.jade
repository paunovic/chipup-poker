extends base
block content
  p="player is "+(online ?"on":"off")+"line"
  table(border=1)
    tr
      th name
    tr
      td=user.displayname
  table(border=1)
    tr
      th club name
    each val in clubs
      tr
        td
          a(href='/club?id='+val._id)=val.name
