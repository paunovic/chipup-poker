extends secure
block content
  table(border=1)
    tr
      th date registered
      th name
    each val in users
      tr
        td=val._id.getTimestamp()
        td
          a(href='/secure/user?id='+val._id)=val.displayname
  div=Math.round(sum/100)
