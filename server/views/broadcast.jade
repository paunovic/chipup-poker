extends secure
block content
  p enter the message below
  div
    form(action='/secure/sendBroadcast',method='post')
      textarea(rows=5,cols=30,name='msg')
      input(type='submit',value='send!')
