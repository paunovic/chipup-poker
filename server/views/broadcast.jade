extends secure
block content
  h1 Enter the message below
  div
    form(action='/secure/sendBroadcast',method='post')
      textarea(rows=5,cols=70,name='msg')
      br
      br
      input(type='submit',value='Send')
