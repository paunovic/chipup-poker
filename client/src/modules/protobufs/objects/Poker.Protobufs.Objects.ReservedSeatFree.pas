// Generated by the protocol buffer compiler.  DO NOT EDIT!
// Source: message.proto

unit Poker.Protobufs.Objects.ReservedSeatFree;

interface

uses
  System.SysUtils,
  {$IFNDEF FPC} System.Generics.Collections {$ELSE} Contnrs {$ENDIF},
  pbOutput, Poker.Protobufs.Objects.Base, Poker.Protobufs.Reader, Poker.Types,
  Poker.Protobufs.Objects.TableStatus;

type
  {$RTTI INHERIT}
  TPB_ReservedSeatFree = class(TProtobufBaseObject)
  private
    const
      kTsFieldNumber = 1;
      kSeatIndexFieldNumber = 2;

    var
      FTs: TPB_TableStatus;
      FSeatIndex: UInt32;
      FHasBits: UINT32;

    procedure set_has_Ts;
    procedure clear_has_Ts;
    procedure SetTs(const AValue: TPB_TableStatus);
    procedure set_has_SeatIndex;
    procedure clear_has_SeatIndex;
    procedure SetSeatIndex(const AValue: UInt32);
  public
    constructor Create(const AFrom: TPB_ReservedSeatFree; const ALightweight: Boolean = FALSE); overload;
    destructor Destroy; override;
    procedure LoadFromProtobufReader(const AProtobufReader: TProtobufReader; const ASize: Integer); override;
    procedure MergeFrom(const AFrom: TPB_ReservedSeatFree);
    procedure Clear;
    function IsInitialized: Boolean; override;

    // required TableStatus Ts = 1;
    function has_Ts: Boolean;
    procedure clear_Ts;

    // required uint32 SeatIndex = 2;
    function has_SeatIndex: Boolean;
    procedure clear_SeatIndex;
  published
    property Ts: TPB_TableStatus read FTs write SetTs;
    property SeatIndex: UInt32 read FSeatIndex write SetSeatIndex;
  end;

  TPB_ReservedSeatFreeList = class(TObjectList<TPB_ReservedSeatFree>)
    procedure Assign(const APB_ReservedSeatFreeList: TList<TPB_ReservedSeatFree>);
  end;

implementation

uses
  pbPublic;


constructor TPB_ReservedSeatFree.Create(const AFrom: TPB_ReservedSeatFree; const ALightweight: Boolean = FALSE);
begin
  inherited Create(ALightweight);
  MergeFrom(AFrom);
end;

destructor TPB_ReservedSeatFree.Destroy;
begin
  if Assigned(FTs) then
    FreeAndNil(FTs);
  inherited;
end;

procedure TPB_ReservedSeatFree.LoadFromProtobufReader(const AProtobufReader: TProtobufReader; const ASize: Integer);
var
  tag, field_number, wire_type, endpos: Integer;
begin
  endpos := AProtobufReader.getPos + ASize;
  while (AProtobufReader.getPos < endpos) and
        (AProtobufReader.GetNext(tag, wire_type, field_number)) do
    case field_number of
      kTsFieldNumber: begin
        Assert(wire_type = WIRETYPE_LENGTH_DELIMITED);
        if not Assigned(FTs) then
          FTs := TPB_TableStatus.Create;
        FTs.LoadFromProtobufReader(AProtobufReader, AProtobufReader.readInt32);
        set_has_Ts;
      end;
      kSeatIndexFieldNumber: begin
        Assert(wire_type = WIRETYPE_VARINT);
        FSeatIndex := AProtobufReader.readUInt32;
        set_has_SeatIndex;
      end;
    else
      AProtobufReader.skipField(tag);
    end;
end;

procedure TPB_ReservedSeatFree.MergeFrom(const AFrom: TPB_ReservedSeatFree);
begin
  if (AFrom.has_Ts) then
    FTs.MergeFrom(AFrom.Ts);
  if AFrom.has_SeatIndex then
    SetSeatIndex(AFrom.SeatIndex);
end;

function TPB_ReservedSeatFree.IsInitialized: Boolean;
begin
  if (FHasBits and $3) <> $3 then
    Exit(FALSE);
  if (has_Ts) then
    if not FTs.IsInitialized then
      Exit(FALSE);
  Exit(TRUE);
end;

procedure TPB_ReservedSeatFree.clear_Ts;
begin
  FreeAndNil(FTs);
  clear_has_Ts;
end;

function TPB_ReservedSeatFree.has_Ts: Boolean;
begin
  result := (FHasBits and 1) > 0;
end;

procedure TPB_ReservedSeatFree.set_has_Ts;
begin
  FHasBits := FHasBits or 1;
end;

procedure TPB_ReservedSeatFree.clear_has_Ts;
begin
  FHasBits := FHasBits and not 1;
end;

procedure TPB_ReservedSeatFree.SetTs(const AValue: TPB_TableStatus);
begin
  if not Lightweight then
    Assert(not has_Ts);
  FTs := AValue;
  if not Lightweight then
    ProtobufOutput.writeMessage(kTsFieldNumber, AValue.ProtobufOutput);
  set_has_Ts;
end;

procedure TPB_ReservedSeatFree.clear_SeatIndex;
begin
  FSeatIndex := 0;
  clear_has_SeatIndex;
end;

function TPB_ReservedSeatFree.has_SeatIndex: Boolean;
begin
  result := (FHasBits and 2) > 0;
end;

procedure TPB_ReservedSeatFree.set_has_SeatIndex;
begin
  FHasBits := FHasBits or 2;
end;

procedure TPB_ReservedSeatFree.clear_has_SeatIndex;
begin
  FHasBits := FHasBits and not 2;
end;

procedure TPB_ReservedSeatFree.SetSeatIndex(const AValue: UInt32);
begin
  if not Lightweight then
    Assert(not has_SeatIndex);
  FSeatIndex := AValue;
  if not Lightweight then
    ProtobufOutput.writeUInt32(kSeatIndexFieldNumber, AValue);
  set_has_SeatIndex;
end;

procedure TPB_ReservedSeatFree.Clear;
begin
  if FHasBits = 0 then
    Exit;

  clear_Ts;
  clear_SeatIndex;
end;

procedure TPB_ReservedSeatFreeList.Assign(const APB_ReservedSeatFreeList: TList<TPB_ReservedSeatFree>);
var
  pbobj: TPB_ReservedSeatFree;
begin
  Clear;
  for pbobj in APB_ReservedSeatFreeList do
    Add(TPB_ReservedSeatFree.Create(pbobj, TRUE));
end;

end.
