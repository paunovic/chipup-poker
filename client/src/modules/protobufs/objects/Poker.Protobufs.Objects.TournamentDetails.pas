// Generated by the protocol buffer compiler.  DO NOT EDIT!
// Source: message.proto

unit Poker.Protobufs.Objects.TournamentDetails;

interface

uses
  System.SysUtils, System.Classes, {$IFNDEF FPC} System.Generics.Collections {$ELSE} Contnrs {$ENDIF}, pbOutput, Poker.Protobufs.Objects.Base, Poker.Protobufs.Reader, Poker.Types;

type
  TPB_TournamentDetails = class(TProtobufBaseObject)
  private
    const
      kIdFieldNumber = 1;

    var
      FId: TMongoId;
      _has_bits_: UINT32;

    procedure set_has_MongoId;
    procedure clear_has_MongoId;
    procedure SetMongoId(const AValue: TMongoId);

  public
    constructor Create(const AFrom: TPB_TournamentDetails; const ALightweight: Boolean = FALSE); overload;
    destructor Destroy; override;
    procedure LoadFromProtobufReader(const AProtobufReader: TProtobufReader; const ASize: Integer); override;
    procedure MergeFrom(const AFrom: TPB_TournamentDetails);
    procedure Clear;
    function IsInitialized: Boolean; override;

    // required bytes MongoId = 1;
    function has_MongoId: Boolean;
    procedure clear_MongoId;
    property MongoId: TMongoId read FId write SetMongoId;

  end;

  TPB_TournamentDetailsList = class(TObjectList<TPB_TournamentDetails>)
    procedure Assign(const APB_TournamentDetailsList: TList<TPB_TournamentDetails>);
  end;

implementation

uses
  pbPublic, Poker.Common.Misc;


constructor TPB_TournamentDetails.Create(const AFrom: TPB_TournamentDetails; const ALightweight: Boolean = FALSE);
begin
  inherited Create(ALightweight);
  MergeFrom(AFrom);
end;

destructor TPB_TournamentDetails.Destroy;
begin
  inherited;
end;

procedure TPB_TournamentDetails.LoadFromProtobufReader(const AProtobufReader: TProtobufReader; const ASize: Integer);
var
  tag, field_number, wire_type, endpos: Integer;
begin
  endpos := AProtobufReader.getPos + ASize;
  while (AProtobufReader.getPos < endpos) and
        (AProtobufReader.GetNext(tag, wire_type, field_number)) do
    case field_number of
      kIdFieldNumber: begin
        Assert(wire_type = WIRETYPE_LENGTH_DELIMITED);
        FId := AProtobufReader.readMongoId;
        set_has_MongoId;
      end;
    else
      AProtobufReader.skipField(tag);
    end;
end;

procedure TPB_TournamentDetails.MergeFrom(const AFrom: TPB_TournamentDetails);
begin
  if AFrom.has_MongoId then
    SetMongoId(AFrom.MongoId);
end;

function TPB_TournamentDetails.IsInitialized: Boolean;
begin
  if (_has_bits_ and $1) <> $1 then
    Exit(FALSE);
  Exit(TRUE);
end;

procedure TPB_TournamentDetails.clear_MongoId;
begin
  FId.Clear;
  clear_has_MongoId;
end;

function TPB_TournamentDetails.has_MongoId: Boolean;
begin
  result := (_has_bits_ and 1) > 0;
end;

procedure TPB_TournamentDetails.set_has_MongoId;
begin
  _has_bits_ := _has_bits_ or 1;
end;

procedure TPB_TournamentDetails.clear_has_MongoId;
begin
  _has_bits_ := _has_bits_ and not 1;
end;

procedure TPB_TournamentDetails.SetMongoId(const AValue: TMongoId);
begin
  Assert(not has_MongoId);
  FId := AValue;
  if not Lightweight then
  begin
    ProtobufOutput.writeTag(kIdFieldNumber, WIRETYPE_LENGTH_DELIMITED);
    ProtobufOutput.writeRawVarint32(12);
    ProtobufOutput.writeRawData(AValue.Memory, 12);
  end;
  set_has_MongoId;
end;

procedure TPB_TournamentDetails.Clear;
begin
  if _has_bits_ = 0 then
    Exit;

  clear_MongoId;
end;

procedure TPB_TournamentDetailsList.Assign(const APB_TournamentDetailsList: TList<TPB_TournamentDetails>);
var
  pbobj: TPB_TournamentDetails;
begin
  Clear;
  for pbobj in APB_TournamentDetailsList do
    Add(TPB_TournamentDetails.Create(pbobj, TRUE));
end;

end.
