// Generated by the protocol buffer compiler.  DO NOT EDIT!
// Source: message.proto

unit Poker.Protobufs.Objects.TournamentTableStart;

interface

uses
  System.SysUtils, System.Classes, {$IFNDEF FPC} System.Generics.Collections {$ELSE} Contnrs {$ENDIF}, pbOutput, Poker.Protobufs.Objects.Base, Poker.Protobufs.Reader, Poker.Types,
  Poker.Protobufs.Objects.Game, Poker.Protobufs.Objects.TableStatus;

type
  TPB_TournamentTableStart = class(TProtobufBaseObject)
  private
    const
      kGameFieldNumber = 1;
      kTableStatusFieldNumber = 2;

    var
      FGame: TPB_Game;
      FTableStatus: TPB_TableStatus;
      _has_bits_: UINT32;

    procedure set_has_Game;
    procedure clear_has_Game;
    procedure SetGame(const AValue: TPB_Game);
    procedure set_has_TableStatus;
    procedure clear_has_TableStatus;
    procedure SetTableStatus(const AValue: TPB_TableStatus);
  public
    constructor Create(const AFrom: TPB_TournamentTableStart; const ALightweight: Boolean = FALSE); overload;
    destructor Destroy; override;
    procedure LoadFromProtobufReader(const AProtobufReader: TProtobufReader; const ASize: Integer); override;
    procedure MergeFrom(const AFrom: TPB_TournamentTableStart);
    procedure Clear;
    function IsInitialized: Boolean; override;

    // required Game Game = 1;
    function has_Game: Boolean;
    procedure clear_Game;
    property Game: TPB_Game read FGame write SetGame;

    // required TableStatus TableStatus = 2;
    function has_TableStatus: Boolean;
    procedure clear_TableStatus;
    property TableStatus: TPB_TableStatus read FTableStatus write SetTableStatus;

  end;

  TPB_TournamentTableStartList = class(TObjectList<TPB_TournamentTableStart>)
    procedure Assign(const APB_TournamentTableStartList: TList<TPB_TournamentTableStart>);
  end;

implementation

uses
  pbPublic, Poker.Common.Misc;


constructor TPB_TournamentTableStart.Create(const AFrom: TPB_TournamentTableStart; const ALightweight: Boolean = FALSE);
begin
  inherited Create(ALightweight);
  MergeFrom(AFrom);
end;

destructor TPB_TournamentTableStart.Destroy;
begin
  if Assigned(FGame) then
    FreeAndNil(FGame);
  if Assigned(FTableStatus) then
    FreeAndNil(FTableStatus);
  inherited;
end;

procedure TPB_TournamentTableStart.LoadFromProtobufReader(const AProtobufReader: TProtobufReader; const ASize: Integer);
var
  tag, field_number, wire_type, endpos: Integer;
begin
  endpos := AProtobufReader.getPos + ASize;
  while (AProtobufReader.getPos < endpos) and
        (AProtobufReader.GetNext(tag, wire_type, field_number)) do
    case field_number of
      kGameFieldNumber: begin
        Assert(wire_type = WIRETYPE_LENGTH_DELIMITED);
        if not Assigned(FGame) then
          FGame := TPB_Game.Create;
        FGame.LoadFromProtobufReader(AProtobufReader, AProtobufReader.readInt32);
        set_has_Game;
      end;
      kTableStatusFieldNumber: begin
        Assert(wire_type = WIRETYPE_LENGTH_DELIMITED);
        if not Assigned(FTableStatus) then
          FTableStatus := TPB_TableStatus.Create;
        FTableStatus.LoadFromProtobufReader(AProtobufReader, AProtobufReader.readInt32);
        set_has_TableStatus;
      end;
    else
      AProtobufReader.skipField(tag);
    end;
end;

procedure TPB_TournamentTableStart.MergeFrom(const AFrom: TPB_TournamentTableStart);
begin
  if (AFrom.has_Game) then
    FGame.MergeFrom(AFrom.Game);
  if (AFrom.has_TableStatus) then
    FTableStatus.MergeFrom(AFrom.TableStatus);
end;

function TPB_TournamentTableStart.IsInitialized: Boolean;
begin
  if (_has_bits_ and $3) <> $3 then
    Exit(FALSE);
  if (has_Game) then
    if not FGame.IsInitialized then
      Exit(FALSE);
  if (has_TableStatus) then
    if not FTableStatus.IsInitialized then
      Exit(FALSE);
  Exit(TRUE);
end;

procedure TPB_TournamentTableStart.clear_Game;
begin
  FreeAndNil(FGame);
  clear_has_Game;
end;

function TPB_TournamentTableStart.has_Game: Boolean;
begin
  result := (_has_bits_ and 1) > 0;
end;

procedure TPB_TournamentTableStart.set_has_Game;
begin
  _has_bits_ := _has_bits_ or 1;
end;

procedure TPB_TournamentTableStart.clear_has_Game;
begin
  _has_bits_ := _has_bits_ and not 1;
end;

procedure TPB_TournamentTableStart.SetGame(const AValue: TPB_Game);
begin
  if not Lightweight then
    Assert(not has_Game);
  FGame := AValue;
  if not Lightweight then
    ProtobufOutput.writeMessage(kGameFieldNumber, AValue.ProtobufOutput);
  set_has_Game;
end;

procedure TPB_TournamentTableStart.clear_TableStatus;
begin
  FreeAndNil(FTableStatus);
  clear_has_TableStatus;
end;

function TPB_TournamentTableStart.has_TableStatus: Boolean;
begin
  result := (_has_bits_ and 2) > 0;
end;

procedure TPB_TournamentTableStart.set_has_TableStatus;
begin
  _has_bits_ := _has_bits_ or 2;
end;

procedure TPB_TournamentTableStart.clear_has_TableStatus;
begin
  _has_bits_ := _has_bits_ and not 2;
end;

procedure TPB_TournamentTableStart.SetTableStatus(const AValue: TPB_TableStatus);
begin
  if not Lightweight then
    Assert(not has_TableStatus);
  FTableStatus := AValue;
  if not Lightweight then
    ProtobufOutput.writeMessage(kTableStatusFieldNumber, AValue.ProtobufOutput);
  set_has_TableStatus;
end;

procedure TPB_TournamentTableStart.Clear;
begin
  if _has_bits_ = 0 then
    Exit;

  clear_Game;
  clear_TableStatus;
end;

procedure TPB_TournamentTableStartList.Assign(const APB_TournamentTableStartList: TList<TPB_TournamentTableStart>);
var
  pbobj: TPB_TournamentTableStart;
begin
  Clear;
  for pbobj in APB_TournamentTableStartList do
    Add(TPB_TournamentTableStart.Create(pbobj, TRUE));
end;

end.
