// Generated by the protocol buffer compiler.  DO NOT EDIT!
// Source: message.proto

unit Poker.Protobufs.Objects.TournamentInfo;

interface

uses
  System.SysUtils, System.Classes, {$IFNDEF FPC} System.Generics.Collections {$ELSE} Contnrs {$ENDIF}, pbOutput, Poker.Protobufs.Objects.Base, Poker.Protobufs.Reader, Poker.Types,
  Poker.Protobufs.Objects.Game, Poker.Protobufs.Objects.TournamentMember, Poker.Protobufs.Objects.GameBlinds;

type
  TTournamentState = (tnsOpen = 0,tnsInProgress = 1,tnsCancelled = 2,tnsOnBreak = 3,tnsStarting = 4);

  TPB_TournamentInfo = class(TProtobufBaseObject)
  private
    const
      kIdFieldNumber = 1;
      kNameFieldNumber = 2;
      kDescriptionFieldNumber = 3;
      kGametypeFieldNumber = 4;
      kLimitFieldNumber = 5;
      kSeatsPerTableFieldNumber = 6;
      kMinplayersFieldNumber = 7;
      kMaxplayersFieldNumber = 8;
      kStartingchipsFieldNumber = 9;
      kTimeperlevelFieldNumber = 10;
      kRegisteredPlayersFieldNumber = 11;
      kStartTimeFieldNumber = 12;
      kPlayersFieldNumber = 13;
      kStateFieldNumber = 14;
      kGamesFieldNumber = 15;
      kBlindStructureFieldNumber = 16;
      kCurrentBlindLevelFieldNumber = 17;
      kCurrentBlindLevelEndTimeFieldNumber = 18;

    var
      FId: TMongoId;
      FName: String;
      FDescription: String;
      FGametype: TGameType;
      FLimit: TGameLimit;
      FSeatsPerTable: UInt32;
      FMinplayers: UInt32;
      FMaxplayers: UInt32;
      FStartingchips: UInt32;
      FTimeperlevel: UInt32;
      FRegisteredPlayers: UInt32;
      FStartTime: UInt32;
      FPlayers: TList<TPB_TournamentMember>;
      FState: TTournamentState;
      FGames: TList<TPB_Game>;
      FBlindStructure: TList<TPB_GameBlinds>;
      FCurrentBlindLevel: UInt32;
      FCurrentBlindLevelEndTime: UInt64;
      _has_bits_: UINT32;

    procedure set_has_MongoId;
    procedure clear_has_MongoId;
    procedure SetMongoId(const AValue: TMongoId);
    procedure set_has_Name;
    procedure clear_has_Name;
    procedure SetName(const AValue: String);
    procedure set_has_Description;
    procedure clear_has_Description;
    procedure SetDescription(const AValue: String);
    procedure set_has_Gametype;
    procedure clear_has_Gametype;
    procedure SetGametype(const AValue: TGameType);
    procedure set_has_Limit;
    procedure clear_has_Limit;
    procedure SetLimit(const AValue: TGameLimit);
    procedure set_has_SeatsPerTable;
    procedure clear_has_SeatsPerTable;
    procedure SetSeatsPerTable(const AValue: UInt32);
    procedure set_has_Minplayers;
    procedure clear_has_Minplayers;
    procedure SetMinplayers(const AValue: UInt32);
    procedure set_has_Maxplayers;
    procedure clear_has_Maxplayers;
    procedure SetMaxplayers(const AValue: UInt32);
    procedure set_has_Startingchips;
    procedure clear_has_Startingchips;
    procedure SetStartingchips(const AValue: UInt32);
    procedure set_has_Timeperlevel;
    procedure clear_has_Timeperlevel;
    procedure SetTimeperlevel(const AValue: UInt32);
    procedure set_has_RegisteredPlayers;
    procedure clear_has_RegisteredPlayers;
    procedure SetRegisteredPlayers(const AValue: UInt32);
    procedure set_has_StartTime;
    procedure clear_has_StartTime;
    procedure SetStartTime(const AValue: UInt32);
    procedure set_has_Players;
    procedure clear_has_Players;
    procedure set_has_State;
    procedure clear_has_State;
    procedure SetState(const AValue: TTournamentState);
    procedure set_has_Games;
    procedure clear_has_Games;
    procedure set_has_BlindStructure;
    procedure clear_has_BlindStructure;
    procedure set_has_CurrentBlindLevel;
    procedure clear_has_CurrentBlindLevel;
    procedure SetCurrentBlindLevel(const AValue: UInt32);
    procedure set_has_CurrentBlindLevelEndTime;
    procedure clear_has_CurrentBlindLevelEndTime;
    procedure SetCurrentBlindLevelEndTime(const AValue: UInt64);
    procedure PlayersNotifyEvent(Sender: TObject; const Item: TPB_TournamentMember; Action: TCollectionNotification);
    procedure GamesNotifyEvent(Sender: TObject; const Item: TPB_Game; Action: TCollectionNotification);
    procedure BlindStructureNotifyEvent(Sender: TObject; const Item: TPB_GameBlinds; Action: TCollectionNotification);

  protected
    procedure InitObjects; override;
    procedure HookNotifiers; override;

  public
    constructor Create(const AFrom: TPB_TournamentInfo; const ALightweight: Boolean = FALSE); overload;
    destructor Destroy; override;
    procedure LoadFromProtobufReader(const AProtobufReader: TProtobufReader; const ASize: Integer); override;
    procedure MergeFrom(const AFrom: TPB_TournamentInfo);
    procedure Clear;
    function IsInitialized: Boolean; override;

    // required bytes MongoId = 1;
    function has_MongoId: Boolean;
    procedure clear_MongoId;
    property MongoId: TMongoId read FId write SetMongoId;

    // required string Name = 2;
    function has_Name: Boolean;
    procedure clear_Name;
    property Name: String read FName write SetName;

    // required string Description = 3;
    function has_Description: Boolean;
    procedure clear_Description;
    property Description: String read FDescription write SetDescription;

    // required GameType Gametype = 4;
    function has_Gametype: Boolean;
    procedure clear_Gametype;
    property Gametype: TGameType read FGametype write SetGametype;

    // required GameLimit Limit = 5;
    function has_Limit: Boolean;
    procedure clear_Limit;
    property Limit: TGameLimit read FLimit write SetLimit;

    // required uint32 SeatsPerTable = 6;
    function has_SeatsPerTable: Boolean;
    procedure clear_SeatsPerTable;
    property SeatsPerTable: UInt32 read FSeatsPerTable write SetSeatsPerTable;

    // required uint32 Minplayers = 7;
    function has_Minplayers: Boolean;
    procedure clear_Minplayers;
    property Minplayers: UInt32 read FMinplayers write SetMinplayers;

    // required uint32 Maxplayers = 8;
    function has_Maxplayers: Boolean;
    procedure clear_Maxplayers;
    property Maxplayers: UInt32 read FMaxplayers write SetMaxplayers;

    // required uint32 Startingchips = 9;
    function has_Startingchips: Boolean;
    procedure clear_Startingchips;
    property Startingchips: UInt32 read FStartingchips write SetStartingchips;

    // required uint32 Timeperlevel = 10;
    function has_Timeperlevel: Boolean;
    procedure clear_Timeperlevel;
    property Timeperlevel: UInt32 read FTimeperlevel write SetTimeperlevel;

    // required uint32 RegisteredPlayers = 11;
    function has_RegisteredPlayers: Boolean;
    procedure clear_RegisteredPlayers;
    property RegisteredPlayers: UInt32 read FRegisteredPlayers write SetRegisteredPlayers;

    // required uint32 StartTime = 12;
    function has_StartTime: Boolean;
    procedure clear_StartTime;
    property StartTime: UInt32 read FStartTime write SetStartTime;

    // repeated TournamentMember Players = 13;
    function has_Players: Boolean;
    procedure clear_Players;
    property Players: TList<TPB_TournamentMember> read FPlayers;

    // required TournamentState State = 14;
    function has_State: Boolean;
    procedure clear_State;
    property State: TTournamentState read FState write SetState;

    // repeated Game Games = 15;
    function has_Games: Boolean;
    procedure clear_Games;
    property Games: TList<TPB_Game> read FGames;

    // repeated GameBlinds BlindStructure = 16;
    function has_BlindStructure: Boolean;
    procedure clear_BlindStructure;
    property BlindStructure: TList<TPB_GameBlinds> read FBlindStructure;

    // optional uint32 CurrentBlindLevel = 17;
    function has_CurrentBlindLevel: Boolean;
    procedure clear_CurrentBlindLevel;
    property CurrentBlindLevel: UInt32 read FCurrentBlindLevel write SetCurrentBlindLevel;

    // optional uint64 CurrentBlindLevelEndTime = 18;
    function has_CurrentBlindLevelEndTime: Boolean;
    procedure clear_CurrentBlindLevelEndTime;
    property CurrentBlindLevelEndTime: UInt64 read FCurrentBlindLevelEndTime write SetCurrentBlindLevelEndTime;

  end;

  TPB_TournamentInfoList = class(TObjectList<TPB_TournamentInfo>)
    procedure Assign(const APB_TournamentInfoList: TList<TPB_TournamentInfo>);
  end;

implementation

uses
  pbPublic, Poker.Common.Misc;


constructor TPB_TournamentInfo.Create(const AFrom: TPB_TournamentInfo; const ALightweight: Boolean = FALSE);
begin
  inherited Create(ALightweight);
  MergeFrom(AFrom);
end;

destructor TPB_TournamentInfo.Destroy;
begin
  if Assigned(FPlayers) then
  begin
    FPlayers.OnNotify := nil;
    FreeAndNil(FPlayers);
  end;
  if Assigned(FGames) then
  begin
    FGames.OnNotify := nil;
    FreeAndNil(FGames);
  end;
  if Assigned(FBlindStructure) then
  begin
    FBlindStructure.OnNotify := nil;
    FreeAndNil(FBlindStructure);
  end;
  inherited;
end;

procedure TPB_TournamentInfo.InitObjects;
begin
  inherited;
  FPlayers := TObjectList<TPB_TournamentMember>.Create;
  FGames := TObjectList<TPB_Game>.Create;
  FBlindStructure := TObjectList<TPB_GameBlinds>.Create;
end;

procedure TPB_TournamentInfo.HookNotifiers;
begin
  inherited;
  FPlayers.OnNotify := PlayersNotifyEvent;
  FGames.OnNotify := GamesNotifyEvent;
  FBlindStructure.OnNotify := BlindStructureNotifyEvent;
end;

procedure TPB_TournamentInfo.LoadFromProtobufReader(const AProtobufReader: TProtobufReader; const ASize: Integer);
var
  tag, field_number, wire_type, endpos: Integer;
begin
  endpos := AProtobufReader.getPos + ASize;
  while (AProtobufReader.getPos < endpos) and
        (AProtobufReader.GetNext(tag, wire_type, field_number)) do
    case field_number of
      kIdFieldNumber: begin
        Assert(wire_type = WIRETYPE_LENGTH_DELIMITED);
        FId := AProtobufReader.readMongoId;
        set_has_MongoId;
      end;
      kNameFieldNumber: begin
        Assert(wire_type = WIRETYPE_LENGTH_DELIMITED);
        FName := AProtobufReader.readUtf8String;
        set_has_Name;
      end;
      kDescriptionFieldNumber: begin
        Assert(wire_type = WIRETYPE_LENGTH_DELIMITED);
        FDescription := AProtobufReader.readUtf8String;
        set_has_Description;
      end;
      kGametypeFieldNumber: begin
        Assert(wire_type = WIRETYPE_VARINT);
        FGametype := TGameType(AProtobufReader.readEnum);
        set_has_Gametype;
      end;
      kLimitFieldNumber: begin
        Assert(wire_type = WIRETYPE_VARINT);
        FLimit := TGameLimit(AProtobufReader.readEnum);
        set_has_Limit;
      end;
      kSeatsPerTableFieldNumber: begin
        Assert(wire_type = WIRETYPE_VARINT);
        FSeatsPerTable := AProtobufReader.readUInt32;
        set_has_SeatsPerTable;
      end;
      kMinplayersFieldNumber: begin
        Assert(wire_type = WIRETYPE_VARINT);
        FMinplayers := AProtobufReader.readUInt32;
        set_has_Minplayers;
      end;
      kMaxplayersFieldNumber: begin
        Assert(wire_type = WIRETYPE_VARINT);
        FMaxplayers := AProtobufReader.readUInt32;
        set_has_Maxplayers;
      end;
      kStartingchipsFieldNumber: begin
        Assert(wire_type = WIRETYPE_VARINT);
        FStartingchips := AProtobufReader.readUInt32;
        set_has_Startingchips;
      end;
      kTimeperlevelFieldNumber: begin
        Assert(wire_type = WIRETYPE_VARINT);
        FTimeperlevel := AProtobufReader.readUInt32;
        set_has_Timeperlevel;
      end;
      kRegisteredPlayersFieldNumber: begin
        Assert(wire_type = WIRETYPE_VARINT);
        FRegisteredPlayers := AProtobufReader.readUInt32;
        set_has_RegisteredPlayers;
      end;
      kStartTimeFieldNumber: begin
        Assert(wire_type = WIRETYPE_VARINT);
        FStartTime := AProtobufReader.readUInt32;
        set_has_StartTime;
      end;
      kPlayersFieldNumber: begin
        Assert(wire_type = WIRETYPE_LENGTH_DELIMITED);
        FPlayers.Add(TPB_TournamentMember.Create(AProtobufReader, AProtobufReader.readInt32, Lightweight));
        set_has_Players;
      end;
      kStateFieldNumber: begin
        Assert(wire_type = WIRETYPE_VARINT);
        FState := TTournamentState(AProtobufReader.readEnum);
        set_has_State;
      end;
      kGamesFieldNumber: begin
        Assert(wire_type = WIRETYPE_LENGTH_DELIMITED);
        FGames.Add(TPB_Game.Create(AProtobufReader, AProtobufReader.readInt32, Lightweight));
        set_has_Games;
      end;
      kBlindStructureFieldNumber: begin
        Assert(wire_type = WIRETYPE_LENGTH_DELIMITED);
        FBlindStructure.Add(TPB_GameBlinds.Create(AProtobufReader, AProtobufReader.readInt32, Lightweight));
        set_has_BlindStructure;
      end;
      kCurrentBlindLevelFieldNumber: begin
        Assert(wire_type = WIRETYPE_VARINT);
        FCurrentBlindLevel := AProtobufReader.readUInt32;
        set_has_CurrentBlindLevel;
      end;
      kCurrentBlindLevelEndTimeFieldNumber: begin
        Assert(wire_type = WIRETYPE_VARINT);
        FCurrentBlindLevelEndTime := AProtobufReader.readInt64;
        set_has_CurrentBlindLevelEndTime;
      end;
    else
      AProtobufReader.skipField(tag);
    end;
end;

procedure TPB_TournamentInfo.MergeFrom(const AFrom: TPB_TournamentInfo);
var
  pbobj12: TPB_TournamentMember;
  pbobj14: TPB_Game;
  pbobj15: TPB_GameBlinds;
begin
  if AFrom.has_MongoId then
    SetMongoId(AFrom.MongoId);
  if AFrom.has_Name then
    SetName(AFrom.Name);
  if AFrom.has_Description then
    SetDescription(AFrom.Description);
  if AFrom.has_Gametype then
    SetGametype(AFrom.Gametype);
  if AFrom.has_Limit then
    SetLimit(AFrom.Limit);
  if AFrom.has_SeatsPerTable then
    SetSeatsPerTable(AFrom.SeatsPerTable);
  if AFrom.has_Minplayers then
    SetMinplayers(AFrom.Minplayers);
  if AFrom.has_Maxplayers then
    SetMaxplayers(AFrom.Maxplayers);
  if AFrom.has_Startingchips then
    SetStartingchips(AFrom.Startingchips);
  if AFrom.has_Timeperlevel then
    SetTimeperlevel(AFrom.Timeperlevel);
  if AFrom.has_RegisteredPlayers then
    SetRegisteredPlayers(AFrom.RegisteredPlayers);
  if AFrom.has_StartTime then
    SetStartTime(AFrom.StartTime);
  for pbobj12 in AFrom.Players do
    FPlayers.Add(TPB_TournamentMember.Create(pbobj12));
  if AFrom.has_State then
    SetState(AFrom.State);
  for pbobj14 in AFrom.Games do
    FGames.Add(TPB_Game.Create(pbobj14));
  for pbobj15 in AFrom.BlindStructure do
    FBlindStructure.Add(TPB_GameBlinds.Create(pbobj15));
  if AFrom.has_CurrentBlindLevel then
    SetCurrentBlindLevel(AFrom.CurrentBlindLevel);
  if AFrom.has_CurrentBlindLevelEndTime then
    SetCurrentBlindLevelEndTime(AFrom.CurrentBlindLevelEndTime);
end;

function TPB_TournamentInfo.IsInitialized: Boolean;
var
  pbobj: TProtobufBaseObject;
begin
  if (_has_bits_ and $2fff) <> $2fff then
    Exit(FALSE);
  for pbobj in Players do
    if not pbobj.IsInitialized then
      Exit(FALSE);
  for pbobj in Games do
    if not pbobj.IsInitialized then
      Exit(FALSE);
  for pbobj in BlindStructure do
    if not pbobj.IsInitialized then
      Exit(FALSE);
  Exit(TRUE);
end;

procedure TPB_TournamentInfo.clear_MongoId;
begin
  FId.Clear;
  clear_has_MongoId;
end;

function TPB_TournamentInfo.has_MongoId: Boolean;
begin
  result := (_has_bits_ and 1) > 0;
end;

procedure TPB_TournamentInfo.set_has_MongoId;
begin
  _has_bits_ := _has_bits_ or 1;
end;

procedure TPB_TournamentInfo.clear_has_MongoId;
begin
  _has_bits_ := _has_bits_ and not 1;
end;

procedure TPB_TournamentInfo.SetMongoId(const AValue: TMongoId);
begin
  if not Lightweight then
    Assert(not has_MongoId);
  FId := AValue;
  if not Lightweight then
  begin
    ProtobufOutput.writeTag(kIdFieldNumber, WIRETYPE_LENGTH_DELIMITED);
    ProtobufOutput.writeRawVarint32(12);
    ProtobufOutput.writeRawData(AValue.Memory, 12);
  end;
  set_has_MongoId;
end;

procedure TPB_TournamentInfo.clear_Name;
begin
  FName := '';
  clear_has_Name;
end;

function TPB_TournamentInfo.has_Name: Boolean;
begin
  result := (_has_bits_ and 2) > 0;
end;

procedure TPB_TournamentInfo.set_has_Name;
begin
  _has_bits_ := _has_bits_ or 2;
end;

procedure TPB_TournamentInfo.clear_has_Name;
begin
  _has_bits_ := _has_bits_ and not 2;
end;

procedure TPB_TournamentInfo.SetName(const AValue: String);
begin
  if not Lightweight then
    Assert(not has_Name);
  FName := AValue;
  if not Lightweight then
    ProtobufOutput.writeString(kNameFieldNumber, AValue);
  set_has_Name;
end;

procedure TPB_TournamentInfo.clear_Description;
begin
  FDescription := '';
  clear_has_Description;
end;

function TPB_TournamentInfo.has_Description: Boolean;
begin
  result := (_has_bits_ and 4) > 0;
end;

procedure TPB_TournamentInfo.set_has_Description;
begin
  _has_bits_ := _has_bits_ or 4;
end;

procedure TPB_TournamentInfo.clear_has_Description;
begin
  _has_bits_ := _has_bits_ and not 4;
end;

procedure TPB_TournamentInfo.SetDescription(const AValue: String);
begin
  if not Lightweight then
    Assert(not has_Description);
  FDescription := AValue;
  if not Lightweight then
    ProtobufOutput.writeString(kDescriptionFieldNumber, AValue);
  set_has_Description;
end;

procedure TPB_TournamentInfo.clear_Gametype;
begin
  FGametype := TGameType(0);
  clear_has_Gametype;
end;

function TPB_TournamentInfo.has_Gametype: Boolean;
begin
  result := (_has_bits_ and 8) > 0;
end;

procedure TPB_TournamentInfo.set_has_Gametype;
begin
  _has_bits_ := _has_bits_ or 8;
end;

procedure TPB_TournamentInfo.clear_has_Gametype;
begin
  _has_bits_ := _has_bits_ and not 8;
end;

procedure TPB_TournamentInfo.SetGametype(const AValue: TGameType);
begin
  if not Lightweight then
    Assert(not has_Gametype);
  FGametype := AValue;
  if not Lightweight then
    ProtobufOutput.writeInt32(kGametypeFieldNumber, Integer(AValue));
  set_has_Gametype;
end;

procedure TPB_TournamentInfo.clear_Limit;
begin
  FLimit := TGameLimit(0);
  clear_has_Limit;
end;

function TPB_TournamentInfo.has_Limit: Boolean;
begin
  result := (_has_bits_ and 16) > 0;
end;

procedure TPB_TournamentInfo.set_has_Limit;
begin
  _has_bits_ := _has_bits_ or 16;
end;

procedure TPB_TournamentInfo.clear_has_Limit;
begin
  _has_bits_ := _has_bits_ and not 16;
end;

procedure TPB_TournamentInfo.SetLimit(const AValue: TGameLimit);
begin
  if not Lightweight then
    Assert(not has_Limit);
  FLimit := AValue;
  if not Lightweight then
    ProtobufOutput.writeInt32(kLimitFieldNumber, Integer(AValue));
  set_has_Limit;
end;

procedure TPB_TournamentInfo.clear_SeatsPerTable;
begin
  FSeatsPerTable := 0;
  clear_has_SeatsPerTable;
end;

function TPB_TournamentInfo.has_SeatsPerTable: Boolean;
begin
  result := (_has_bits_ and 32) > 0;
end;

procedure TPB_TournamentInfo.set_has_SeatsPerTable;
begin
  _has_bits_ := _has_bits_ or 32;
end;

procedure TPB_TournamentInfo.clear_has_SeatsPerTable;
begin
  _has_bits_ := _has_bits_ and not 32;
end;

procedure TPB_TournamentInfo.SetSeatsPerTable(const AValue: UInt32);
begin
  if not Lightweight then
    Assert(not has_SeatsPerTable);
  FSeatsPerTable := AValue;
  if not Lightweight then
    ProtobufOutput.writeUInt32(kSeatsPerTableFieldNumber, AValue);
  set_has_SeatsPerTable;
end;

procedure TPB_TournamentInfo.clear_Minplayers;
begin
  FMinplayers := 0;
  clear_has_Minplayers;
end;

function TPB_TournamentInfo.has_Minplayers: Boolean;
begin
  result := (_has_bits_ and 64) > 0;
end;

procedure TPB_TournamentInfo.set_has_Minplayers;
begin
  _has_bits_ := _has_bits_ or 64;
end;

procedure TPB_TournamentInfo.clear_has_Minplayers;
begin
  _has_bits_ := _has_bits_ and not 64;
end;

procedure TPB_TournamentInfo.SetMinplayers(const AValue: UInt32);
begin
  if not Lightweight then
    Assert(not has_Minplayers);
  FMinplayers := AValue;
  if not Lightweight then
    ProtobufOutput.writeUInt32(kMinplayersFieldNumber, AValue);
  set_has_Minplayers;
end;

procedure TPB_TournamentInfo.clear_Maxplayers;
begin
  FMaxplayers := 0;
  clear_has_Maxplayers;
end;

function TPB_TournamentInfo.has_Maxplayers: Boolean;
begin
  result := (_has_bits_ and 128) > 0;
end;

procedure TPB_TournamentInfo.set_has_Maxplayers;
begin
  _has_bits_ := _has_bits_ or 128;
end;

procedure TPB_TournamentInfo.clear_has_Maxplayers;
begin
  _has_bits_ := _has_bits_ and not 128;
end;

procedure TPB_TournamentInfo.SetMaxplayers(const AValue: UInt32);
begin
  if not Lightweight then
    Assert(not has_Maxplayers);
  FMaxplayers := AValue;
  if not Lightweight then
    ProtobufOutput.writeUInt32(kMaxplayersFieldNumber, AValue);
  set_has_Maxplayers;
end;

procedure TPB_TournamentInfo.clear_Startingchips;
begin
  FStartingchips := 0;
  clear_has_Startingchips;
end;

function TPB_TournamentInfo.has_Startingchips: Boolean;
begin
  result := (_has_bits_ and 256) > 0;
end;

procedure TPB_TournamentInfo.set_has_Startingchips;
begin
  _has_bits_ := _has_bits_ or 256;
end;

procedure TPB_TournamentInfo.clear_has_Startingchips;
begin
  _has_bits_ := _has_bits_ and not 256;
end;

procedure TPB_TournamentInfo.SetStartingchips(const AValue: UInt32);
begin
  if not Lightweight then
    Assert(not has_Startingchips);
  FStartingchips := AValue;
  if not Lightweight then
    ProtobufOutput.writeUInt32(kStartingchipsFieldNumber, AValue);
  set_has_Startingchips;
end;

procedure TPB_TournamentInfo.clear_Timeperlevel;
begin
  FTimeperlevel := 0;
  clear_has_Timeperlevel;
end;

function TPB_TournamentInfo.has_Timeperlevel: Boolean;
begin
  result := (_has_bits_ and 512) > 0;
end;

procedure TPB_TournamentInfo.set_has_Timeperlevel;
begin
  _has_bits_ := _has_bits_ or 512;
end;

procedure TPB_TournamentInfo.clear_has_Timeperlevel;
begin
  _has_bits_ := _has_bits_ and not 512;
end;

procedure TPB_TournamentInfo.SetTimeperlevel(const AValue: UInt32);
begin
  if not Lightweight then
    Assert(not has_Timeperlevel);
  FTimeperlevel := AValue;
  if not Lightweight then
    ProtobufOutput.writeUInt32(kTimeperlevelFieldNumber, AValue);
  set_has_Timeperlevel;
end;

procedure TPB_TournamentInfo.clear_RegisteredPlayers;
begin
  FRegisteredPlayers := 0;
  clear_has_RegisteredPlayers;
end;

function TPB_TournamentInfo.has_RegisteredPlayers: Boolean;
begin
  result := (_has_bits_ and 1024) > 0;
end;

procedure TPB_TournamentInfo.set_has_RegisteredPlayers;
begin
  _has_bits_ := _has_bits_ or 1024;
end;

procedure TPB_TournamentInfo.clear_has_RegisteredPlayers;
begin
  _has_bits_ := _has_bits_ and not 1024;
end;

procedure TPB_TournamentInfo.SetRegisteredPlayers(const AValue: UInt32);
begin
  if not Lightweight then
    Assert(not has_RegisteredPlayers);
  FRegisteredPlayers := AValue;
  if not Lightweight then
    ProtobufOutput.writeUInt32(kRegisteredPlayersFieldNumber, AValue);
  set_has_RegisteredPlayers;
end;

procedure TPB_TournamentInfo.clear_StartTime;
begin
  FStartTime := 0;
  clear_has_StartTime;
end;

function TPB_TournamentInfo.has_StartTime: Boolean;
begin
  result := (_has_bits_ and 2048) > 0;
end;

procedure TPB_TournamentInfo.set_has_StartTime;
begin
  _has_bits_ := _has_bits_ or 2048;
end;

procedure TPB_TournamentInfo.clear_has_StartTime;
begin
  _has_bits_ := _has_bits_ and not 2048;
end;

procedure TPB_TournamentInfo.SetStartTime(const AValue: UInt32);
begin
  if not Lightweight then
    Assert(not has_StartTime);
  FStartTime := AValue;
  if not Lightweight then
    ProtobufOutput.writeUInt32(kStartTimeFieldNumber, AValue);
  set_has_StartTime;
end;

procedure TPB_TournamentInfo.clear_Players;
var
  on_notify: TCollectionNotifyEvent<TPB_TournamentMember>;
begin
  on_notify := FPlayers.OnNotify;
  FPlayers.OnNotify := nil;
  FPlayers.Clear;
  FPlayers.OnNotify := on_notify;
  clear_has_Players;
end;

function TPB_TournamentInfo.has_Players: Boolean;
begin
  result := (_has_bits_ and 4096) > 0;
end;

procedure TPB_TournamentInfo.set_has_Players;
begin
  _has_bits_ := _has_bits_ or 4096;
end;

procedure TPB_TournamentInfo.clear_has_Players;
begin
  _has_bits_ := _has_bits_ and not 4096;
end;

procedure TPB_TournamentInfo.PlayersNotifyEvent(Sender: TObject; const Item: TPB_TournamentMember; Action: TCollectionNotification);
begin
  Assert(Action = cnAdded);
  set_has_Players;
  if not Lightweight then
  begin
    ProtobufOutput.writeTag(kPlayersFieldNumber,WIRETYPE_LENGTH_DELIMITED);
    ProtobufOutput.writeRawVarint32(Item.ProtobufOutput.getSerializedSize);
    Item.ProtobufOutput.writeTo(ProtobufOutput);
  end;
end;

procedure TPB_TournamentInfo.clear_State;
begin
  FState := TTournamentState(0);
  clear_has_State;
end;

function TPB_TournamentInfo.has_State: Boolean;
begin
  result := (_has_bits_ and 8192) > 0;
end;

procedure TPB_TournamentInfo.set_has_State;
begin
  _has_bits_ := _has_bits_ or 8192;
end;

procedure TPB_TournamentInfo.clear_has_State;
begin
  _has_bits_ := _has_bits_ and not 8192;
end;

procedure TPB_TournamentInfo.SetState(const AValue: TTournamentState);
begin
  if not Lightweight then
    Assert(not has_State);
  FState := AValue;
  if not Lightweight then
    ProtobufOutput.writeInt32(kStateFieldNumber, Integer(AValue));
  set_has_State;
end;

procedure TPB_TournamentInfo.clear_Games;
var
  on_notify: TCollectionNotifyEvent<TPB_Game>;
begin
  on_notify := FGames.OnNotify;
  FGames.OnNotify := nil;
  FGames.Clear;
  FGames.OnNotify := on_notify;
  clear_has_Games;
end;

function TPB_TournamentInfo.has_Games: Boolean;
begin
  result := (_has_bits_ and 16384) > 0;
end;

procedure TPB_TournamentInfo.set_has_Games;
begin
  _has_bits_ := _has_bits_ or 16384;
end;

procedure TPB_TournamentInfo.clear_has_Games;
begin
  _has_bits_ := _has_bits_ and not 16384;
end;

procedure TPB_TournamentInfo.GamesNotifyEvent(Sender: TObject; const Item: TPB_Game; Action: TCollectionNotification);
begin
  Assert(Action = cnAdded);
  set_has_Games;
  if not Lightweight then
  begin
    ProtobufOutput.writeTag(kGamesFieldNumber,WIRETYPE_LENGTH_DELIMITED);
    ProtobufOutput.writeRawVarint32(Item.ProtobufOutput.getSerializedSize);
    Item.ProtobufOutput.writeTo(ProtobufOutput);
  end;
end;

procedure TPB_TournamentInfo.clear_BlindStructure;
var
  on_notify: TCollectionNotifyEvent<TPB_GameBlinds>;
begin
  on_notify := FBlindStructure.OnNotify;
  FBlindStructure.OnNotify := nil;
  FBlindStructure.Clear;
  FBlindStructure.OnNotify := on_notify;
  clear_has_BlindStructure;
end;

function TPB_TournamentInfo.has_BlindStructure: Boolean;
begin
  result := (_has_bits_ and 32768) > 0;
end;

procedure TPB_TournamentInfo.set_has_BlindStructure;
begin
  _has_bits_ := _has_bits_ or 32768;
end;

procedure TPB_TournamentInfo.clear_has_BlindStructure;
begin
  _has_bits_ := _has_bits_ and not 32768;
end;

procedure TPB_TournamentInfo.BlindStructureNotifyEvent(Sender: TObject; const Item: TPB_GameBlinds; Action: TCollectionNotification);
begin
  Assert(Action = cnAdded);
  set_has_BlindStructure;
  if not Lightweight then
  begin
    ProtobufOutput.writeTag(kBlindStructureFieldNumber,WIRETYPE_LENGTH_DELIMITED);
    ProtobufOutput.writeRawVarint32(Item.ProtobufOutput.getSerializedSize);
    Item.ProtobufOutput.writeTo(ProtobufOutput);
  end;
end;

procedure TPB_TournamentInfo.clear_CurrentBlindLevel;
begin
  FCurrentBlindLevel := 0;
  clear_has_CurrentBlindLevel;
end;

function TPB_TournamentInfo.has_CurrentBlindLevel: Boolean;
begin
  result := (_has_bits_ and 65536) > 0;
end;

procedure TPB_TournamentInfo.set_has_CurrentBlindLevel;
begin
  _has_bits_ := _has_bits_ or 65536;
end;

procedure TPB_TournamentInfo.clear_has_CurrentBlindLevel;
begin
  _has_bits_ := _has_bits_ and not 65536;
end;

procedure TPB_TournamentInfo.SetCurrentBlindLevel(const AValue: UInt32);
begin
  if not Lightweight then
    Assert(not has_CurrentBlindLevel);
  FCurrentBlindLevel := AValue;
  if not Lightweight then
    ProtobufOutput.writeUInt32(kCurrentBlindLevelFieldNumber, AValue);
  set_has_CurrentBlindLevel;
end;

procedure TPB_TournamentInfo.clear_CurrentBlindLevelEndTime;
begin
  FCurrentBlindLevelEndTime := 0;
  clear_has_CurrentBlindLevelEndTime;
end;

function TPB_TournamentInfo.has_CurrentBlindLevelEndTime: Boolean;
begin
  result := (_has_bits_ and 131072) > 0;
end;

procedure TPB_TournamentInfo.set_has_CurrentBlindLevelEndTime;
begin
  _has_bits_ := _has_bits_ or 131072;
end;

procedure TPB_TournamentInfo.clear_has_CurrentBlindLevelEndTime;
begin
  _has_bits_ := _has_bits_ and not 131072;
end;

procedure TPB_TournamentInfo.SetCurrentBlindLevelEndTime(const AValue: UInt64);
begin
  if not Lightweight then
    Assert(not has_CurrentBlindLevelEndTime);
  FCurrentBlindLevelEndTime := AValue;
  if not Lightweight then
    ProtobufOutput.WriteInt64(kCurrentBlindLevelEndTimeFieldNumber, AValue);
  set_has_CurrentBlindLevelEndTime;
end;

procedure TPB_TournamentInfo.Clear;
begin
  if _has_bits_ = 0 then
    Exit;

  clear_MongoId;
  clear_Name;
  clear_Description;
  clear_Gametype;
  clear_Limit;
  clear_SeatsPerTable;
  clear_Minplayers;
  clear_Maxplayers;
  clear_Startingchips;
  clear_Timeperlevel;
  clear_RegisteredPlayers;
  clear_StartTime;
  clear_Players;
  clear_State;
  clear_Games;
  clear_BlindStructure;
  clear_CurrentBlindLevel;
  clear_CurrentBlindLevelEndTime;
end;

procedure TPB_TournamentInfoList.Assign(const APB_TournamentInfoList: TList<TPB_TournamentInfo>);
var
  pbobj: TPB_TournamentInfo;
begin
  Clear;
  for pbobj in APB_TournamentInfoList do
    Add(TPB_TournamentInfo.Create(pbobj, TRUE));
end;

end.
