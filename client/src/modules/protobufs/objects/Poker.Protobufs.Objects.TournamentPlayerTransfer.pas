// Generated by the protocol buffer compiler.  DO NOT EDIT!
// Source: message.proto

unit Poker.Protobufs.Objects.TournamentPlayerTransfer;

interface

uses
  System.SysUtils,
  {$IFNDEF FPC} System.Generics.Collections {$ELSE} Contnrs {$ENDIF},
  pbOutput, Poker.Protobufs.Objects.Base, Poker.Protobufs.Reader, Poker.Types;

type
  TPB_TournamentPlayerTransfer = class(TProtobufBaseObject)
  private
    const
      kGameSourceFieldNumber = 1;
      kGameDestinationFieldNumber = 2;
      kUserIdFieldNumber = 3;
      kSeatSourceFieldNumber = 4;
      kSeatDestinationFieldNumber = 5;

    var
      FGameSource: TMongoId;
      FGameDestination: TMongoId;
      FUserId: TMongoId;
      FSeatSource: UInt32;
      FSeatDestination: UInt32;
      FHasBits: UINT32;

    procedure set_has_GameSource;
    procedure clear_has_GameSource;
    procedure SetGameSource(const AValue: TMongoId);
    procedure set_has_GameDestination;
    procedure clear_has_GameDestination;
    procedure SetGameDestination(const AValue: TMongoId);
    procedure set_has_UserId;
    procedure clear_has_UserId;
    procedure SetUserId(const AValue: TMongoId);
    procedure set_has_SeatSource;
    procedure clear_has_SeatSource;
    procedure SetSeatSource(const AValue: UInt32);
    procedure set_has_SeatDestination;
    procedure clear_has_SeatDestination;
    procedure SetSeatDestination(const AValue: UInt32);
  public
    constructor Create(const AFrom: TPB_TournamentPlayerTransfer; const ALightweight: Boolean = FALSE); overload;
    destructor Destroy; override;
    procedure LoadFromProtobufReader(const AProtobufReader: TProtobufReader; const ASize: Integer); override;
    procedure MergeFrom(const AFrom: TPB_TournamentPlayerTransfer);
    procedure Clear;
    function IsInitialized: Boolean; override;

    // required bytes GameSource = 1;
    function has_GameSource: Boolean;
    procedure clear_GameSource;
    property GameSource: TMongoId read FGameSource write SetGameSource;

    // required bytes GameDestination = 2;
    function has_GameDestination: Boolean;
    procedure clear_GameDestination;
    property GameDestination: TMongoId read FGameDestination write SetGameDestination;

    // required bytes UserId = 3;
    function has_UserId: Boolean;
    procedure clear_UserId;
    property UserId: TMongoId read FUserId write SetUserId;

    // required uint32 SeatSource = 4;
    function has_SeatSource: Boolean;
    procedure clear_SeatSource;
    property SeatSource: UInt32 read FSeatSource write SetSeatSource;

    // required uint32 SeatDestination = 5;
    function has_SeatDestination: Boolean;
    procedure clear_SeatDestination;
    property SeatDestination: UInt32 read FSeatDestination write SetSeatDestination;
  end;

  TPB_TournamentPlayerTransferList = class(TObjectList<TPB_TournamentPlayerTransfer>)
    procedure Assign(const APB_TournamentPlayerTransferList: TList<TPB_TournamentPlayerTransfer>);
  end;

implementation

uses
  pbPublic;


constructor TPB_TournamentPlayerTransfer.Create(const AFrom: TPB_TournamentPlayerTransfer; const ALightweight: Boolean = FALSE);
begin
  inherited Create(ALightweight);
  MergeFrom(AFrom);
end;

destructor TPB_TournamentPlayerTransfer.Destroy;
begin
  inherited;
end;

procedure TPB_TournamentPlayerTransfer.LoadFromProtobufReader(const AProtobufReader: TProtobufReader; const ASize: Integer);
var
  tag, field_number, wire_type, endpos: Integer;
begin
  endpos := AProtobufReader.getPos + ASize;
  while (AProtobufReader.getPos < endpos) and
        (AProtobufReader.GetNext(tag, wire_type, field_number)) do
    case field_number of
      kGameSourceFieldNumber: begin
        Assert(wire_type = WIRETYPE_LENGTH_DELIMITED);
        FGameSource := AProtobufReader.readMongoId;
        set_has_GameSource;
      end;
      kGameDestinationFieldNumber: begin
        Assert(wire_type = WIRETYPE_LENGTH_DELIMITED);
        FGameDestination := AProtobufReader.readMongoId;
        set_has_GameDestination;
      end;
      kUserIdFieldNumber: begin
        Assert(wire_type = WIRETYPE_LENGTH_DELIMITED);
        FUserId := AProtobufReader.readMongoId;
        set_has_UserId;
      end;
      kSeatSourceFieldNumber: begin
        Assert(wire_type = WIRETYPE_VARINT);
        FSeatSource := AProtobufReader.readUInt32;
        set_has_SeatSource;
      end;
      kSeatDestinationFieldNumber: begin
        Assert(wire_type = WIRETYPE_VARINT);
        FSeatDestination := AProtobufReader.readUInt32;
        set_has_SeatDestination;
      end;
    else
      AProtobufReader.skipField(tag);
    end;
end;

procedure TPB_TournamentPlayerTransfer.MergeFrom(const AFrom: TPB_TournamentPlayerTransfer);
begin
  if AFrom.has_GameSource then
    SetGameSource(AFrom.GameSource);
  if AFrom.has_GameDestination then
    SetGameDestination(AFrom.GameDestination);
  if AFrom.has_UserId then
    SetUserId(AFrom.UserId);
  if AFrom.has_SeatSource then
    SetSeatSource(AFrom.SeatSource);
  if AFrom.has_SeatDestination then
    SetSeatDestination(AFrom.SeatDestination);
end;

function TPB_TournamentPlayerTransfer.IsInitialized: Boolean;
begin
  if (FHasBits and $1f) <> $1f then
    Exit(FALSE);
  Exit(TRUE);
end;

procedure TPB_TournamentPlayerTransfer.clear_GameSource;
begin
  FGameSource.Clear;
  clear_has_GameSource;
end;

function TPB_TournamentPlayerTransfer.has_GameSource: Boolean;
begin
  result := (FHasBits and 1) > 0;
end;

procedure TPB_TournamentPlayerTransfer.set_has_GameSource;
begin
  FHasBits := FHasBits or 1;
end;

procedure TPB_TournamentPlayerTransfer.clear_has_GameSource;
begin
  FHasBits := FHasBits and not 1;
end;

procedure TPB_TournamentPlayerTransfer.SetGameSource(const AValue: TMongoId);
begin
  if not Lightweight then
    Assert(not has_GameSource);
  FGameSource := AValue;
  if not Lightweight then
  begin
    ProtobufOutput.writeTag(kGameSourceFieldNumber, WIRETYPE_LENGTH_DELIMITED);
    ProtobufOutput.writeRawVarint32(12);
    ProtobufOutput.writeRawData(AValue.Memory, 12);
  end;
  set_has_GameSource;
end;

procedure TPB_TournamentPlayerTransfer.clear_GameDestination;
begin
  FGameDestination.Clear;
  clear_has_GameDestination;
end;

function TPB_TournamentPlayerTransfer.has_GameDestination: Boolean;
begin
  result := (FHasBits and 2) > 0;
end;

procedure TPB_TournamentPlayerTransfer.set_has_GameDestination;
begin
  FHasBits := FHasBits or 2;
end;

procedure TPB_TournamentPlayerTransfer.clear_has_GameDestination;
begin
  FHasBits := FHasBits and not 2;
end;

procedure TPB_TournamentPlayerTransfer.SetGameDestination(const AValue: TMongoId);
begin
  if not Lightweight then
    Assert(not has_GameDestination);
  FGameDestination := AValue;
  if not Lightweight then
  begin
    ProtobufOutput.writeTag(kGameDestinationFieldNumber, WIRETYPE_LENGTH_DELIMITED);
    ProtobufOutput.writeRawVarint32(12);
    ProtobufOutput.writeRawData(AValue.Memory, 12);
  end;
  set_has_GameDestination;
end;

procedure TPB_TournamentPlayerTransfer.clear_UserId;
begin
  FUserId.Clear;
  clear_has_UserId;
end;

function TPB_TournamentPlayerTransfer.has_UserId: Boolean;
begin
  result := (FHasBits and 4) > 0;
end;

procedure TPB_TournamentPlayerTransfer.set_has_UserId;
begin
  FHasBits := FHasBits or 4;
end;

procedure TPB_TournamentPlayerTransfer.clear_has_UserId;
begin
  FHasBits := FHasBits and not 4;
end;

procedure TPB_TournamentPlayerTransfer.SetUserId(const AValue: TMongoId);
begin
  if not Lightweight then
    Assert(not has_UserId);
  FUserId := AValue;
  if not Lightweight then
  begin
    ProtobufOutput.writeTag(kUserIdFieldNumber, WIRETYPE_LENGTH_DELIMITED);
    ProtobufOutput.writeRawVarint32(12);
    ProtobufOutput.writeRawData(AValue.Memory, 12);
  end;
  set_has_UserId;
end;

procedure TPB_TournamentPlayerTransfer.clear_SeatSource;
begin
  FSeatSource := 0;
  clear_has_SeatSource;
end;

function TPB_TournamentPlayerTransfer.has_SeatSource: Boolean;
begin
  result := (FHasBits and 8) > 0;
end;

procedure TPB_TournamentPlayerTransfer.set_has_SeatSource;
begin
  FHasBits := FHasBits or 8;
end;

procedure TPB_TournamentPlayerTransfer.clear_has_SeatSource;
begin
  FHasBits := FHasBits and not 8;
end;

procedure TPB_TournamentPlayerTransfer.SetSeatSource(const AValue: UInt32);
begin
  if not Lightweight then
    Assert(not has_SeatSource);
  FSeatSource := AValue;
  if not Lightweight then
    ProtobufOutput.writeUInt32(kSeatSourceFieldNumber, AValue);
  set_has_SeatSource;
end;

procedure TPB_TournamentPlayerTransfer.clear_SeatDestination;
begin
  FSeatDestination := 0;
  clear_has_SeatDestination;
end;

function TPB_TournamentPlayerTransfer.has_SeatDestination: Boolean;
begin
  result := (FHasBits and 16) > 0;
end;

procedure TPB_TournamentPlayerTransfer.set_has_SeatDestination;
begin
  FHasBits := FHasBits or 16;
end;

procedure TPB_TournamentPlayerTransfer.clear_has_SeatDestination;
begin
  FHasBits := FHasBits and not 16;
end;

procedure TPB_TournamentPlayerTransfer.SetSeatDestination(const AValue: UInt32);
begin
  if not Lightweight then
    Assert(not has_SeatDestination);
  FSeatDestination := AValue;
  if not Lightweight then
    ProtobufOutput.writeUInt32(kSeatDestinationFieldNumber, AValue);
  set_has_SeatDestination;
end;

procedure TPB_TournamentPlayerTransfer.Clear;
begin
  if FHasBits = 0 then
    Exit;

  clear_GameSource;
  clear_GameDestination;
  clear_UserId;
  clear_SeatSource;
  clear_SeatDestination;
end;

procedure TPB_TournamentPlayerTransferList.Assign(const APB_TournamentPlayerTransferList: TList<TPB_TournamentPlayerTransfer>);
var
  pbobj: TPB_TournamentPlayerTransfer;
begin
  Clear;
  for pbobj in APB_TournamentPlayerTransferList do
    Add(TPB_TournamentPlayerTransfer.Create(pbobj, TRUE));
end;

end.
