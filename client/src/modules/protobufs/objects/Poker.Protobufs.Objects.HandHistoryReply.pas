// Generated by the protocol buffer compiler.  DO NOT EDIT!
// Source: message.proto

unit Poker.Protobufs.Objects.HandHistoryReply;

interface

uses
  System.SysUtils,
  {$IFNDEF FPC} System.Generics.Collections {$ELSE} Contnrs {$ENDIF},
  pbOutput, Poker.Protobufs.Objects.Base, Poker.Protobufs.Reader, Poker.Types,
  Poker.Protobufs.Objects.HandHistory;

type
  TPB_HandHistoryReply = class(TProtobufBaseObject)
  private
    const
      kClubIdFieldNumber = 1;
      kGameidFieldNumber = 2;
      kRowsFieldNumber = 3;
      kTournamentIdFieldNumber = 4;

    var
      FClubId: TMongoId;
      FGameid: TMongoId;
      FRows: TList<TPB_HandHistory>;
      FTournamentId: TMongoId;
      FHasBits: UINT32;

    procedure set_has_ClubId;
    procedure clear_has_ClubId;
    procedure SetClubId(const AValue: TMongoId);
    procedure set_has_Gameid;
    procedure clear_has_Gameid;
    procedure SetGameid(const AValue: TMongoId);
    procedure set_has_Rows;
    procedure clear_has_Rows;
    procedure set_has_TournamentId;
    procedure clear_has_TournamentId;
    procedure SetTournamentId(const AValue: TMongoId);
    procedure RowsNotifyEvent(Sender: TObject; const Item: TPB_HandHistory; Action: TCollectionNotification);
  protected
    procedure InitObjects; override;
    procedure HookNotifiers; override;
  public
    constructor Create(const AFrom: TPB_HandHistoryReply; const ALightweight: Boolean = FALSE); overload;
    destructor Destroy; override;
    procedure LoadFromProtobufReader(const AProtobufReader: TProtobufReader; const ASize: Integer); override;
    procedure MergeFrom(const AFrom: TPB_HandHistoryReply);
    procedure Clear;
    function IsInitialized: Boolean; override;

    // optional bytes ClubId = 1;
    function has_ClubId: Boolean;
    procedure clear_ClubId;
    property ClubId: TMongoId read FClubId write SetClubId;

    // required bytes Gameid = 2;
    function has_Gameid: Boolean;
    procedure clear_Gameid;
    property Gameid: TMongoId read FGameid write SetGameid;

    // repeated HandHistory Rows = 3;
    function has_Rows: Boolean;
    procedure clear_Rows;
    property Rows: TList<TPB_HandHistory> read FRows;

    // optional bytes TournamentId = 4;
    function has_TournamentId: Boolean;
    procedure clear_TournamentId;
    property TournamentId: TMongoId read FTournamentId write SetTournamentId;
  end;

  TPB_HandHistoryReplyList = class(TObjectList<TPB_HandHistoryReply>)
    procedure Assign(const APB_HandHistoryReplyList: TList<TPB_HandHistoryReply>);
  end;

implementation

uses
  pbPublic;


constructor TPB_HandHistoryReply.Create(const AFrom: TPB_HandHistoryReply; const ALightweight: Boolean = FALSE);
begin
  inherited Create(ALightweight);
  MergeFrom(AFrom);
end;

destructor TPB_HandHistoryReply.Destroy;
begin
  if Assigned(FRows) then
  begin
    FRows.OnNotify := nil;
    FreeAndNil(FRows);
  end;
  inherited;
end;

procedure TPB_HandHistoryReply.InitObjects;
begin
  inherited;
  FRows := TObjectList<TPB_HandHistory>.Create;
end;

procedure TPB_HandHistoryReply.HookNotifiers;
begin
  inherited;
  FRows.OnNotify := RowsNotifyEvent;
end;

procedure TPB_HandHistoryReply.LoadFromProtobufReader(const AProtobufReader: TProtobufReader; const ASize: Integer);
var
  tag, field_number, wire_type, endpos: Integer;
begin
  endpos := AProtobufReader.getPos + ASize;
  while (AProtobufReader.getPos < endpos) and
        (AProtobufReader.GetNext(tag, wire_type, field_number)) do
    case field_number of
      kClubIdFieldNumber: begin
        Assert(wire_type = WIRETYPE_LENGTH_DELIMITED);
        FClubId := AProtobufReader.readMongoId;
        set_has_ClubId;
      end;
      kGameidFieldNumber: begin
        Assert(wire_type = WIRETYPE_LENGTH_DELIMITED);
        FGameid := AProtobufReader.readMongoId;
        set_has_Gameid;
      end;
      kRowsFieldNumber: begin
        Assert(wire_type = WIRETYPE_LENGTH_DELIMITED);
        FRows.Add(TPB_HandHistory.Create(AProtobufReader, AProtobufReader.readInt32, Lightweight));
        set_has_Rows;
      end;
      kTournamentIdFieldNumber: begin
        Assert(wire_type = WIRETYPE_LENGTH_DELIMITED);
        FTournamentId := AProtobufReader.readMongoId;
        set_has_TournamentId;
      end;
    else
      AProtobufReader.skipField(tag);
    end;
end;

procedure TPB_HandHistoryReply.MergeFrom(const AFrom: TPB_HandHistoryReply);
var
  pbobj2: TPB_HandHistory;
begin
  if AFrom.has_ClubId then
    SetClubId(AFrom.ClubId);
  if AFrom.has_Gameid then
    SetGameid(AFrom.Gameid);
  for pbobj2 in AFrom.Rows do
    FRows.Add(TPB_HandHistory.Create(pbobj2, Lightweight));
  if AFrom.has_TournamentId then
    SetTournamentId(AFrom.TournamentId);
end;

function TPB_HandHistoryReply.IsInitialized: Boolean;
var
  pbobj: TProtobufBaseObject;
begin
  if (FHasBits and $2) <> $2 then
    Exit(FALSE);
  for pbobj in Rows do
    if not pbobj.IsInitialized then
      Exit(FALSE);
  Exit(TRUE);
end;

procedure TPB_HandHistoryReply.clear_ClubId;
begin
  FClubId.Clear;
  clear_has_ClubId;
end;

function TPB_HandHistoryReply.has_ClubId: Boolean;
begin
  result := (FHasBits and 1) > 0;
end;

procedure TPB_HandHistoryReply.set_has_ClubId;
begin
  FHasBits := FHasBits or 1;
end;

procedure TPB_HandHistoryReply.clear_has_ClubId;
begin
  FHasBits := FHasBits and not 1;
end;

procedure TPB_HandHistoryReply.SetClubId(const AValue: TMongoId);
begin
  if not Lightweight then
    Assert(not has_ClubId);
  FClubId := AValue;
  if not Lightweight then
  begin
    ProtobufOutput.writeTag(kClubIdFieldNumber, WIRETYPE_LENGTH_DELIMITED);
    ProtobufOutput.writeRawVarint32(12);
    ProtobufOutput.writeRawData(AValue.Memory, 12);
  end;
  set_has_ClubId;
end;

procedure TPB_HandHistoryReply.clear_Gameid;
begin
  FGameid.Clear;
  clear_has_Gameid;
end;

function TPB_HandHistoryReply.has_Gameid: Boolean;
begin
  result := (FHasBits and 2) > 0;
end;

procedure TPB_HandHistoryReply.set_has_Gameid;
begin
  FHasBits := FHasBits or 2;
end;

procedure TPB_HandHistoryReply.clear_has_Gameid;
begin
  FHasBits := FHasBits and not 2;
end;

procedure TPB_HandHistoryReply.SetGameid(const AValue: TMongoId);
begin
  if not Lightweight then
    Assert(not has_Gameid);
  FGameid := AValue;
  if not Lightweight then
  begin
    ProtobufOutput.writeTag(kGameidFieldNumber, WIRETYPE_LENGTH_DELIMITED);
    ProtobufOutput.writeRawVarint32(12);
    ProtobufOutput.writeRawData(AValue.Memory, 12);
  end;
  set_has_Gameid;
end;

procedure TPB_HandHistoryReply.clear_Rows;
var
  on_notify: TCollectionNotifyEvent<TPB_HandHistory>;
begin
  on_notify := FRows.OnNotify;
  FRows.OnNotify := nil;
  FRows.Clear;
  FRows.OnNotify := on_notify;
  clear_has_Rows;
end;

function TPB_HandHistoryReply.has_Rows: Boolean;
begin
  result := (FHasBits and 4) > 0;
end;

procedure TPB_HandHistoryReply.set_has_Rows;
begin
  FHasBits := FHasBits or 4;
end;

procedure TPB_HandHistoryReply.clear_has_Rows;
begin
  FHasBits := FHasBits and not 4;
end;

procedure TPB_HandHistoryReply.RowsNotifyEvent(Sender: TObject; const Item: TPB_HandHistory; Action: TCollectionNotification);
begin
  Assert(Action = cnAdded);
  set_has_Rows;
  if not Lightweight then
  begin
    ProtobufOutput.writeTag(kRowsFieldNumber,WIRETYPE_LENGTH_DELIMITED);
    ProtobufOutput.writeRawVarint32(Item.ProtobufOutput.getSerializedSize);
    Item.ProtobufOutput.writeTo(ProtobufOutput);
  end;
end;

procedure TPB_HandHistoryReply.clear_TournamentId;
begin
  FTournamentId.Clear;
  clear_has_TournamentId;
end;

function TPB_HandHistoryReply.has_TournamentId: Boolean;
begin
  result := (FHasBits and 8) > 0;
end;

procedure TPB_HandHistoryReply.set_has_TournamentId;
begin
  FHasBits := FHasBits or 8;
end;

procedure TPB_HandHistoryReply.clear_has_TournamentId;
begin
  FHasBits := FHasBits and not 8;
end;

procedure TPB_HandHistoryReply.SetTournamentId(const AValue: TMongoId);
begin
  if not Lightweight then
    Assert(not has_TournamentId);
  FTournamentId := AValue;
  if not Lightweight then
  begin
    ProtobufOutput.writeTag(kTournamentIdFieldNumber, WIRETYPE_LENGTH_DELIMITED);
    ProtobufOutput.writeRawVarint32(12);
    ProtobufOutput.writeRawData(AValue.Memory, 12);
  end;
  set_has_TournamentId;
end;

procedure TPB_HandHistoryReply.Clear;
begin
  if FHasBits = 0 then
    Exit;

  clear_ClubId;
  clear_Gameid;
  clear_Rows;
  clear_TournamentId;
end;

procedure TPB_HandHistoryReplyList.Assign(const APB_HandHistoryReplyList: TList<TPB_HandHistoryReply>);
var
  pbobj: TPB_HandHistoryReply;
begin
  Clear;
  for pbobj in APB_HandHistoryReplyList do
    Add(TPB_HandHistoryReply.Create(pbobj, TRUE));
end;

end.
