// Generated by the protocol buffer compiler.  DO NOT EDIT!
// Source: message.proto

unit Poker.Protobufs.Objects.DeleteTableStats;

interface

uses
  System.SysUtils,
  {$IFNDEF FPC} System.Generics.Collections {$ELSE} Contnrs {$ENDIF},
  pbOutput, Poker.Protobufs.Objects.Base, Poker.Protobufs.Reader, Poker.Types;

type
  {$RTTI INHERIT}
  TPB_DeleteTableStats = class(TProtobufBaseObject)
  private
    const
      kClubIdFieldNumber = 1;
      kTableIdFieldNumber = 2;

    var
      FClubId: TMongoId;
      FTableId: TList<TMongoId>;
      FHasBits: UINT32;

    procedure set_has_ClubId;
    procedure clear_has_ClubId;
    procedure SetClubId(const AValue: TMongoId);
    procedure set_has_TableId;
    procedure clear_has_TableId;
    procedure TableIdNotifyEvent(Sender: TObject; const Item: TMongoId; Action: TCollectionNotification);
  protected
    procedure InitObjects; override;
    procedure HookNotifiers; override;
  public
    constructor Create(const AFrom: TPB_DeleteTableStats; const ALightweight: Boolean = FALSE); overload;
    destructor Destroy; override;
    procedure LoadFromProtobufReader(const AProtobufReader: TProtobufReader; const ASize: Integer); override;
    procedure MergeFrom(const AFrom: TPB_DeleteTableStats);
    procedure Clear;
    function IsInitialized: Boolean; override;

    // required bytes ClubId = 1;
    function has_ClubId: Boolean;
    procedure clear_ClubId;

    // repeated bytes TableId = 2;
    function has_TableId: Boolean;
    procedure clear_TableId;
  published
    property ClubId: TMongoId read FClubId write SetClubId;
    property TableId: TList<TMongoId> read FTableId;
  end;

  TPB_DeleteTableStatsList = class(TObjectList<TPB_DeleteTableStats>)
    procedure Assign(const APB_DeleteTableStatsList: TList<TPB_DeleteTableStats>);
  end;

implementation

uses
  pbPublic;


constructor TPB_DeleteTableStats.Create(const AFrom: TPB_DeleteTableStats; const ALightweight: Boolean = FALSE);
begin
  inherited Create(ALightweight);
  MergeFrom(AFrom);
end;

destructor TPB_DeleteTableStats.Destroy;
begin
  if Assigned(FTableId) then
  begin
    FTableId.OnNotify := nil;
    FreeAndNil(FTableId);
  end;
  inherited;
end;

procedure TPB_DeleteTableStats.InitObjects;
begin
  inherited;
  FTableId := TList<TMongoId>.Create;
end;

procedure TPB_DeleteTableStats.HookNotifiers;
begin
  inherited;
  FTableId.OnNotify := TableIdNotifyEvent;
end;

procedure TPB_DeleteTableStats.LoadFromProtobufReader(const AProtobufReader: TProtobufReader; const ASize: Integer);
var
  tag, field_number, wire_type, endpos: Integer;
begin
  endpos := AProtobufReader.getPos + ASize;
  while (AProtobufReader.getPos < endpos) and
        (AProtobufReader.GetNext(tag, wire_type, field_number)) do
    case field_number of
      kClubIdFieldNumber: begin
        Assert(wire_type = WIRETYPE_LENGTH_DELIMITED);
        FClubId := AProtobufReader.readMongoId;
        set_has_ClubId;
      end;
      kTableIdFieldNumber: begin
        Assert(wire_type = WIRETYPE_LENGTH_DELIMITED);
        FTableId.Add(AProtobufReader.readMongoId);
        set_has_TableId;
      end;
    else
      AProtobufReader.skipField(tag);
    end;
end;

procedure TPB_DeleteTableStats.MergeFrom(const AFrom: TPB_DeleteTableStats);
begin
  if AFrom.has_ClubId then
    SetClubId(AFrom.ClubId);
  FTableId.AddRange(AFrom.TableId);
end;

function TPB_DeleteTableStats.IsInitialized: Boolean;
begin
  if (FHasBits and $1) <> $1 then
    Exit(FALSE);
  Exit(TRUE);
end;

procedure TPB_DeleteTableStats.clear_ClubId;
begin
  FClubId.Clear;
  clear_has_ClubId;
end;

function TPB_DeleteTableStats.has_ClubId: Boolean;
begin
  result := (FHasBits and 1) > 0;
end;

procedure TPB_DeleteTableStats.set_has_ClubId;
begin
  FHasBits := FHasBits or 1;
end;

procedure TPB_DeleteTableStats.clear_has_ClubId;
begin
  FHasBits := FHasBits and not 1;
end;

procedure TPB_DeleteTableStats.SetClubId(const AValue: TMongoId);
begin
  if not Lightweight then
    Assert(not has_ClubId);
  FClubId := AValue;
  if not Lightweight then
  begin
    ProtobufOutput.writeTag(kClubIdFieldNumber, WIRETYPE_LENGTH_DELIMITED);
    ProtobufOutput.writeRawVarint32(12);
    ProtobufOutput.writeRawData(AValue.Memory, 12);
  end;
  set_has_ClubId;
end;

procedure TPB_DeleteTableStats.clear_TableId;
var
  on_notify: TCollectionNotifyEvent<TMongoId>;
begin
  on_notify := FTableId.OnNotify;
  FTableId.OnNotify := nil;
  FTableId.Clear;
  FTableId.OnNotify := on_notify;
  clear_has_TableId;
end;

function TPB_DeleteTableStats.has_TableId: Boolean;
begin
  result := (FHasBits and 2) > 0;
end;

procedure TPB_DeleteTableStats.set_has_TableId;
begin
  FHasBits := FHasBits or 2;
end;

procedure TPB_DeleteTableStats.clear_has_TableId;
begin
  FHasBits := FHasBits and not 2;
end;

procedure TPB_DeleteTableStats.TableIdNotifyEvent(Sender: TObject; const Item: TMongoId; Action: TCollectionNotification);
begin
  Assert(Action = cnAdded);
  set_has_TableId;
  if not Lightweight then
  begin
    ProtobufOutput.writeTag(kTableIdFieldNumber, WIRETYPE_LENGTH_DELIMITED);
    ProtobufOutput.writeRawVarint32(12);
    ProtobufOutput.writeRawData(Item.Memory, 12);
  end;
end;

procedure TPB_DeleteTableStats.Clear;
begin
  if FHasBits = 0 then
    Exit;

  clear_ClubId;
  clear_TableId;
end;

procedure TPB_DeleteTableStatsList.Assign(const APB_DeleteTableStatsList: TList<TPB_DeleteTableStats>);
var
  pbobj: TPB_DeleteTableStats;
begin
  Clear;
  for pbobj in APB_DeleteTableStatsList do
    Add(TPB_DeleteTableStats.Create(pbobj, TRUE));
end;

end.
